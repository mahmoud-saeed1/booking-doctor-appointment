import { ISectionTilel } from "@/app/_interfaces";
import "./index.css";

const SectionTitle = ({ className, title, children }: ISectionTilel) => {
  return <h1>SectionTitle</h1>;
};

export default SectionTitle;
